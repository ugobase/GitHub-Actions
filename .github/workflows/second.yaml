name: Shell action

on: push

jobs:
    second-job:
        runs-on: ubuntu-latest
        steps:
            - name: shellscripting
              run: |
               chmod +x ascii.sh
               ./ascii.sh